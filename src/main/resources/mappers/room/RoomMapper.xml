<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.plateer.fittingroombo.room.mapper.RoomMapper">
    <insert id="insertRoom" useGeneratedKeys="true">
        insert into tbl_room (r_name, r_address, r_detail_address, r_postcode, r_create_dt)
        values(#{rName}, #{rAddress}, #{rDetailAddress}, #{rPostcode}, #{rCreateDt});
    </insert>
</mapper>